# ADR-0003: ライフタイム注釈は書かない

## Status
Accepted

## Context
Rustではライフタイム注釈（`'a`）を呼び出し側・定義側の両方に書く必要があり、学習コストが高い。

## Decision
ライフタイムはコンパイラが所有権DAGから自動推論する。ユーザーがライフタイム注釈を書く構文は存在しない。

## Consequences
「所有権はライブラリ作者側だけが意識する」というコンセプトを実現できる。ただし、コンパイラの推論が難しいケースでは制約が生じる可能性がある。
